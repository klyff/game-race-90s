# Teste car-1 neste Cursor

Gerar a tira do Marauder aqui, com GenerateImage. Sem outra máquina, sem SVG, sem o motor.

1. Confirmar âncoras 00 / 08 / 16 / 24 (frente ≠ traseira, lados opostos).
2. `fit-redrawn.ts`: raw → 64×64, margem 4px, origin pinado.
3. Gerar os 28 + 2 HQ.
4. `qa-strip` + `pack-redrawn` sem `--install`.
5. Corrida: farol no nariz; spoiler na mala. Só então instalar.
