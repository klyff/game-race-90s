# car-1 pilot

Empty on purpose. Paste `.cursor/skills/iso-car-strip/CLAUDE_PROMPT.md` into Claude Code with `tools/spritegen/handoff/car-1/`.

After 00–31 + HQ exist:

```bash
npm run gen:qa-strip -- car-1
npm run gen:pack-redrawn -- car-1
```

`--install` only after the race check. Do not run it from the drawing agent.
