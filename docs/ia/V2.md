# NPC AI V2

Investigation pack: `/tmp/NPC-AI-V2-20260818-190758/` (not in git).

## Proven before the change

- Packs park at shared `s`. Solo expert/bobo complete Basin I/II.
- Integrity 0.3 made every reference profile select `RECOVER`.
- Near-wall candidate IDs collapsed.
- Naive kinematics cannot replace `stepVehicle`.

## What shipped

- `RECOVER` is a control mode with enter/exit/hysteresis/`reason=`, including `NO_FORWARD_PROGRESS`.
- Planner: live `(s0,d0)` corridor futures → `stepVehicle` rollout → RaceCore + bounded personality bias.
- Intention labels the winning future. RivalTraits no longer dive independently.
- New traits default from existing weights: `riskTolerance`, `raceFocus`, `patience`, `commitment`.

Profile number tuning is still last. Gate A is “the car moves and wants to finish”.

Post-change 90s mix 2:2:2 on Thunder Basin II: **0/6** cars labelled RECOVER (was 5/6). Solo KLYFF still 2 laps RACE. OVERTAKE appears. Ramp `s≈280` still slows some cars — that is track/airborne, not the fake recover label.

